# CDP Script Format

This file is a compatibility pointer.

The canonical script-format reference lives in
`skills/writing-cdp-scripts/references/script-format.md`.

That reference is the maintained source of truth for `cdpscript` and `cdp run`.
Keep command descriptions there, not in this file.
